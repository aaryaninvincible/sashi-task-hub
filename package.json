{
  "name": "sashi-task-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready full-stack Team Task Manager with React, Express, MongoDB, Mongoose, and JWT role-based auth.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --include=dev --prefix backend && npm install --include=dev --prefix frontend",
    "postinstall": "npm run install:all",
    "build": "npm run build --prefix frontend",
    "railway:build": "npm install && npm run build",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
